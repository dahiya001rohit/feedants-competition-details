# Optional. By default the app calls port 4000 on the machine running Expo (works for web,
# simulators and Expo Go on the same Wi-Fi). Set this if the API runs elsewhere.
EXPO_PUBLIC_API_URL=http://192.168.1.10:4000
